import 'package:flutter/material.dart';

/// Common border radius used across the app widgets.
const BorderRadius borderRadiusMd = BorderRadius.all(Radius.circular(12));

/// Default shape applied to most components.
const RoundedRectangleBorder shapeMd = RoundedRectangleBorder(
  borderRadius: borderRadiusMd,
);
